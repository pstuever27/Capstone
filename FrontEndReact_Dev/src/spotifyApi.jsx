import React from "react";
import { useState } from "react";

const spotifyApi = () => {

    const refreshToken = async () => {
        
    } 

    //Have makeRequest function that encapsulates all the other functions
    const makeRequest = async (urlOptions, code) => {
        
    };

};